# Production Environment - Hostinger Node.js App
# IMPORTANT: Replace ALL values before deployment
# DO NOT commit this file to Git - use this as a template

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Get these values from Hostinger PostgreSQL Database settings
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL="postgresql://your_db_user:your_db_password@your_db_host.hostinger.com:5432/boq_production?sslmode=require"

# ============================================
# JWT CONFIGURATION
# ============================================
# Generate a secure secret using:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# MUST be at least 64 characters!
JWT_SECRET="REPLACE_WITH_SECURE_RANDOM_STRING_MINIMUM_64_CHARACTERS_GENERATED_VIA_CRYPTO"
JWT_EXPIRES_IN="7d"

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=3000
NODE_ENV="production"

# ============================================
# CORS CONFIGURATION
# ============================================
# Set this to your ACTUAL frontend domain after deployment
# For multiple domains, use comma-separated: "https://domain1.com,https://domain2.com"
FRONTEND_URL="https://boq.yourdomain.com"

# ============================================
# SEED DATA - INITIAL USER PASSWORDS
# ============================================
# CRITICAL: Change these immediately after first deployment!
# These are used ONLY for initial database seeding

SEED_ADMIN_EMAIL="admin@badarexpo.com"
SEED_ADMIN_PASSWORD="CHANGE_IMMEDIATELY_AFTER_DEPLOYMENT"

# Department User Passwords
SEED_TECH_PASSWORD="CHANGE_IMMEDIATELY_AFTER_DEPLOYMENT"
SEED_LOGISTICS_PASSWORD="CHANGE_IMMEDIATELY_AFTER_DEPLOYMENT"
SEED_OPERATIONS_PASSWORD="CHANGE_IMMEDIATELY_AFTER_DEPLOYMENT"
SEED_HR_PASSWORD="CHANGE_IMMEDIATELY_AFTER_DEPLOYMENT"

# ============================================
# DEPLOYMENT NOTES
# ============================================
# 1. Copy this file content to Hostinger Node.js App Environment Variables
# 2. Replace ALL placeholder values with actual production values
# 3. Generate strong JWT_SECRET (minimum 64 characters)
# 4. Use strong passwords for all SEED_*_PASSWORD values
# 5. Update FRONTEND_URL after frontend deployment
# 6. Ensure DATABASE_URL includes sslmode=require for security
